import { ProfileNamePageTemplate } from '@/features/settings/components/server/ProfileNamePageTemplate'

export default function ProfileNamePage(
  _props: PageProps<'/settings/profile/name'>
) {
  return <ProfileNamePageTemplate />
}
